<div class="jumbotron">
  <i id="icon-success" class="fa fa-thumbs-up"></i>
  <h1>Done!</h1>
  <br />

  <p class="lead">
    <% if @user.response['cardholder']['kyc_passed'] %>
      Your new Shift card is on its way to <%= @user.city %>.
    <% else %>
      Your application has been submitted. Shift will likely reach out via email.
    <% end %>
  </p>

  <p>
    <div class="btn-padded">
      <%= link_to 'Sign up another...', root_path, class: 'btn btn-default'  %>
    </div>
  </p>
</div>
